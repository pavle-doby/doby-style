# doby-style-cli

CLI Wizard for basic SCSS style folder structure.

> SCSS style structure in less than 10 seconds.

## Usage

Run on of these to start Doby Style CLI Wizard

```bash
npx doby-style-cli
npx doby-style
npx ds-cli
npx ds
```
